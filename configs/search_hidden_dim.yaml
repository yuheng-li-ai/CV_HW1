search_type: grid
num_trials: null

base_config: ./configs/baseline.yaml

grid:
  model.activation: [relu]
  model.hidden_dim: [64, 128, 256, 512]
  train.learning_rate: [0.3]
  train.weight_decay: [0.0001]

notes:
  - Hidden-dimension comparison under the stronger ReLU setting.
  - Learning rate and weight decay are fixed based on earlier experiments.
